<nav class="navbar">
  	<div class="container">
  		<div class="navbar-header">
	  		<%= link_to image_tag("logo-search-orange.png", size: "48x48"), '/', class: "navbar-brand", id: "logo" %>
	  		<%= render '/layouts/header/search' %>
	  	</div>
	  	<div id="navbar">
		  	<ul class="nav navbar-nav navbar-right">
			    <li id="header-link-feed"><%= link_to 'Feed', root_url %></a></li>
			    <li id="header-link-networks"><%= link_to 'Networks', networks_url %></a></li>
			    <li id="header-link-coins"><%= link_to 'Coins', coins_url %></a></li>
			    <li id="header-link-news"><%= link_to 'Links', links_url %></a></li>
			    <li id="header-link-sales"><a>Upcoming sales</a></li>
			    <li id="header-link-account">
			    	<% if user_signed_in? %>
		    			<%= link_to user_path(current_user), class: 'user-activity' do   %>
			    			<%= current_user.avatar.nil? ? "" : image_tag(current_user.avatar, size: '30x30', class: 'avatar') %> 
			    			<p><%= current_user.username unless current_user.username.nil? %></p>
		    			<% end %>
		    			<% if current_user.username.nil? %>
		    				<%= link_to current_user.id.to_s, class: 'header-link-account-glyphicon' do %>
			    				<span class="glyphicon glyphicon-cog" aria-hidden="true"></span>
			    			<% end %>
		    			<% else %>
			    			<%= link_to current_user.username, class: 'header-link-account-glyphicon' do %>
			    				<span class="glyphicon glyphicon-cog" aria-hidden="true"></span>
			    			<% end %>
			    		<% end %>
		    			<%= link_to destroy_user_session_path, method: :delete, class: 'header-link-account-glyphicon logout' do %>
		    				<span class="glyphicon glyphicon-off" aria-hidden="true"></span>
		    			<% end %>
			    	<% else %>
			    		<%= link_to 'login', new_user_registration_url, class: 'login-link' %>
					<% end %>
			    </li>
			</ul>
		</div>
	</div>
</nav>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>