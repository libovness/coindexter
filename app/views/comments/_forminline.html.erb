<%= form_for [@comment, Comment.new] do |f| %>
    <%= f.text_area :body, placeholder: "Add a Reply", rows: 4 %><br/>
    <div class="row">
    	<div class="col-md-12">
    		<%= f.submit "Reply"  %>
    	</div>
    </div>
<% end %>