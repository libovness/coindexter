<% if @success %>
	$(".action-links > li:first-child").html("<%= escape_javascript link_to('Following #{@coin.name}', unfollow_network_coin_path(@coin.network,@coin), remote: true, class: 'action-link', id: 'following-coin', accesskey: 'F') %>");
	$("#following-coin").text("Following " + "<%= escape_javascript @coin.name %>");
<% else %>

<% end %>