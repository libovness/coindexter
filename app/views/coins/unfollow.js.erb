<% if @success %>
	$(".action-links > li:first-child").html("<%= escape_javascript link_to('Follow #{@network.name}', follow_network_path, remote: true, class: 'action-link', id: 'follow-network', accesskey: 'F') %>");
	$("#follow-network").text("Follow " + "<%= escape_javascript @network.name %>");
<% else %>
<% end %>