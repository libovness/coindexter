<% if @success %>
	$(".action-links > li:first-child").html("<%= escape_javascript link_to('Follow #{@network.name}', follow_network_path, remote: true, method: :put, class: 'action-link', id: 'follow-coin', accesskey: 'F') %>");
	$("#follow-coin").text("Follow " + "<%= escape_javascript @network.name %>");
<% else %>
<% end %>